<!-- lwa:meta
{
  "id": "CHAT-12",
  "kind": "issue",
  "title": "Align the GitHub README opening with local conversation capture",
  "authority": "local-native",
  "revision": 2,
  "status": "done",
  "created_at": "2026-09-20T11:45:48Z",
  "updated_at": "2026-09-20T11:53:14Z",
  "owner": {
    "kind": "project",
    "id": "CHAT-P1"
  },
  "relations": []
}
-->
# Align the GitHub README opening with local conversation capture

## Outcome

Authorize a strictly textual opening rewrite of the public GitHub README so it
leads with the same simple product idea now live on the ChatMD onepager:
ChatMD saves a ChatGPT conversation as a Markdown file on the user's own
computer.

This Work does not change product behavior. It changes only the README
opening, from the title through the end of `## What it does`.

## Governing authority / source of truth

CHAT-P1 defines the product purpose, local-first boundary, no-rewrite
invariant, and the one-URL public-share interface. This Work does not change
that Project contract.

Accepted CHAT-11 established the public onepager framing and explicitly did
not change `README.md`. Accepted CHAT-6 owns the public GitHub README as a
separate landing page and remains closed. This Work does not reopen CHAT-6
or CHAT-11.

The current `chatmd.py` and `test_chatmd.py` implementation are the technical
source of truth for product behavior. The owner-approved README opening in
Scope is the source of truth for the authorized copy.

CHAT-D1 remains the active portable bundle contract. The current output does
not implement it.

## Scope

Replace `README.md` from the title through the end of `## What it does` with
this owner-approved text:

~~~~markdown
# chatmd

ChatMD saves a ChatGPT conversation as a Markdown file on your own computer.

You create a ChatGPT share link and give that link to ChatMD. ChatMD uses the
link to retrieve the conversation and saves it as Markdown in your local vault.
Supported user images are downloaded and saved next to that file. ChatMD does
not summarize, paraphrase, or rewrite the conversation. After a successful
save, the local Markdown and image files do not depend on the public share
remaining available.

## What it does

Given one public ChatGPT share URL, ChatMD fetches the share over ordinary HTTP,
reconstructs the active visible conversation from ChatGPT's structured share
data, and writes a deterministic Markdown file into a local vault directory.
Supported visible user images are saved beside the Markdown file in the version
available through the public share.

Successful captures currently go to:

```text
~/My vault/Sources/ChatMD/YYYY/MM/
```

That capture root is machine-specific. After one local install, ChatMD is invoked
as `chatmd`.
~~~~

Leave `## Quick start` and everything below it byte-for-byte unchanged.

## Non-goals

Do not:

- modify product code or tests;
- modify the website;
- address stale `og:image:alt` or `og.png`;
- reopen CHAT-5, CHAT-6, or CHAT-11;
- implement CHAT-D1;
- change Project, Document, or prior Work contracts beyond the navigation
  generated by render;
- invent capabilities, install commands, or distribution status;
- commit or push.

## Evidence requirements

- The README opening matches the owner-approved text in Scope.
- `git diff -- README.md` shows only that opening region.
- `## Quick start` through end of README is byte-for-byte unchanged.
- `git diff --check` passes.
- Local-native render and validate succeed after this Work is added.
- Product files, tests, and website files are unchanged by this Work.

Unittest, pyright, and ruff are not required unless product files change,
which would place the diff outside this Work. No LWA GO run is required;
this Work is documentation and authority only.

## Acceptance criteria

- [x] `README.md` leads with saving a ChatGPT conversation as a local
      Markdown file rather than webpage transience.
- [x] The opening states that a ChatGPT share link is required input.
- [x] Supported user images are described as saved locally in the version
      available through the public share.
- [x] The opening states that ChatMD does not summarize, paraphrase, or
      rewrite the conversation.
- [x] The opening states that after a successful save, the local Markdown
      and image files do not depend on the public share remaining available.
- [x] `## Quick start` and everything below it is unchanged.
- [x] Product code, tests, and website files are unchanged.
- [x] `git diff --check` and local-native render/validate pass.

## Completion boundary

CHAT-12 is complete when the README opening matches the owner-approved text,
Quick start through end of file is unchanged, the required verification
passes, and a human accepts the copy.

Do not mark this Work done, check acceptance criteria as human-accepted,
reconcile authority, commit, or push unless a later human decision
explicitly authorizes that action.

## Implementation evidence

The accepted product implementation is the local working-tree diff against
HEAD `96d185f9b4c97d6a922f0ce039ba5f551eb4043c`. It is not yet committed.
The implementation change is exactly the `README.md` opening, from the
title through the end of `## What it does`.

Authority closeout also changes:

- `authority/Projects/CHAT-P1/Work/CHAT-12.md`;
- render-owned navigation on `authority/Projects/CHAT-P1/CHAT-P1.md`.

The README now leads with:

```text
ChatMD saves a ChatGPT conversation as a Markdown file on your own computer.
```

`## Quick start` through end of file remains byte-for-byte identical to
HEAD. Product files, tests, and website files are unchanged. CHAT-6 and
CHAT-11 were not reopened. No CHAT-D1 bundle work was introduced.

Before acceptance, the CHAT-12 governing-authority sentence was corrected
to state that CHAT-D1 remains the active portable bundle contract and that
the current output does not implement it.

## Verification

No prepared LWA GO run exists for CHAT-12. Closeout uses the copy
contract, `git diff --check`, and authority render/validate. Absence of a
GO bundle does not decide acceptance. Unittest, pyright, and ruff are not
required because no product files changed.

Recorded local verification:

- `git diff -- README.md` - opening region only;
- `## Quick start` through EOF - identical to HEAD, 7404 bytes;
- `git diff --check` - passed, exit 0;
- `lwa render` / `lwa validate` - ChatMD authority root valid
  (1 project, 12 issues, 1 document, 14 objects).

No public conversation body, share identifier, credential, cookie, or
signed URL is retained in tracked evidence.

## Human acceptance

Accepted at `2026-09-20T11:53:14Z` as complete for the CHAT-12 revision 2
boundary.

The human operator reviewed the implemented README opening, the CHAT-D1
wording correction, and the verified CHAT-12 contract, then explicitly
granted acceptance of CHAT-12 and authorized authority reconciliation
without commit or push.

## Disposition

`CHAT-12` is completed.

The public GitHub README now leads with saving a ChatGPT conversation as a
Markdown file on the user's own computer, still requires a ChatGPT share
link as the capture path, and leaves the existing technical documentation
below `## Quick start` unchanged.
<!-- lwa:derived:start -->
## Object state

- ID: `CHAT-12`
- Kind: `issue`
- Status: `done`
- Revision: `2`
- Authority: `local-native`
- Owner: [[Projects/CHAT-P1/CHAT-P1|CHAT-P1]]: chatmd

## Owned Documents

_None._

## Relations

_None._

## Backlinks

_None._
<!-- lwa:derived:end -->
